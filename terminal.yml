theme: oxocarbon

window:
  title: "zihan@dev:~"
  style: macos
  autoHeight: true

terminal:
  prompt: "$ "
  fontSize: 15

animation:
  loop: true

blocks:
  - block: custom
    config:
      command: "./zihan --status"
      lines:
        - "focus      developer tools / LLVM"
        - "building   Soft Sensor Workbench / SpaFlow"
        - "shipping   AgentGate / Gridstone"

  - block: custom
    config:
      command: "cat current_obsessions.txt"
      lines:
        - "LLVM"
        - "compiler internals"
        - "making weird systems useful"
